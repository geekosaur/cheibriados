#!/usr/bin/env perl
use strict;
use warnings;
use lib 'lib';
use Crawl::Bot;

Crawl::Bot->new(
    server   => 'irc.libera.chat',
    port     => 6697,
    ssl      => 1,
    channels => ['#xmonad-unlogged', '#xmonad'],
    nick     => 'xmonadtrack',
    name     => 'the Crawl Bot modified for XMonad, owned by geekosaur',
)->run();
