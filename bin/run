#!/usr/bin/env perl
use strict;
use warnings;
use lib 'lib';
use Crawl::Bot;

Crawl::Bot->new(
    server   => 'chat.freenode.net',
    port     => 6697,
    ssl      => 1,
    channels => ['##crawl-dev', '##crawl'],
    nick     => 'Cheibriados',
    name     => 'Cheibriados the Crawl Bot',
)->run();
