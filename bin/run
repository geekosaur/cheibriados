#!/usr/bin/env perl
use strict;
use warnings;
use lib 'lib';
use Crawl::Bot;

Crawl::Bot->new(
    server   => 'irc.libera.chat',
    port     => 6697,
    ssl      => 1,
    channels => ['#xmonad-unlogged'], # '##crawl-dev', '##crawl', 
    nick     => 'xmonadtrack',
    name     => 'Testing version of Cheibriados the Crawl Bot modified for XMonad',
)->run();
